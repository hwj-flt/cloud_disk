server:
  port: 8280
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_storage?serverTimezone=UTC
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
    druid:
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      stat-view-servlet:
        allow: true
mybatis:
  mapper-locations:
    - classpath*:mapper/*.xml
space:
  size: 1000
upload:
  path: upload/